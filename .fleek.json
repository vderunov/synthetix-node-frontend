{
  "build": {
    "image": "node:alpine",
    "command": "yarn install && yarn start && echo $SOME_ENV",
    "publicDir": "dist",
    "baseDir": ".",
    "environment": {
      "SOME_ENV": "Build finished!",
      "API_URL": "http://localhost:3005/"
    }
  }
}
